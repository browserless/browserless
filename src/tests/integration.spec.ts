
describe('Browserless Chrome', () => {
  describe('WebSockets', () => {
    it('runs requests concurrently', () => {

    });

    it('queues requests', () => {

    });

    it('fails requests', () => {

    });

    it('runs uploaded code', () => {

    });

    it('closes chrome when complete', () => {

    });
  });
});
