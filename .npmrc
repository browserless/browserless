# Supply-chain hardening: do not resolve dependency versions newer than 3 days.
# Requires npm >= 11.10.0 (older npm silently ignores this); see engines.npm.
min-release-age=3
# Exempt our own published scope (we own @browserless.io on the public registry):
min-release-age-exclude[]=@browserless.io/*
